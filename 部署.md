要从现在这个“单机本地 JSON 文件”版本，变成“多用户、数据库存储、账号隔离、公开可访问”的正式版本，接下来本质上要做四层改造：身份、数据、接口、安全。

**先说当前状态**
现在的系统是：
- 无真实登录
- 所有数据共用一份本地 `app-state.json`
- 没有用户隔离
- 后端适合本地调试，不适合公网长期运行

如果要上线，建议按下面顺序做，不要一次全堆上去。

**第一步：先做用户体系**
这是最先要落的，因为后面所有任务、灵感、成就都要挂到用户上。

你需要增加：
- 用户表 `users`
- 登录注册流程
- 会话机制

建议方案：
- 登录方式先用最简单的 `邮箱 + 密码`
- 密码不能明文存，必须哈希，常用 `bcrypt`
- 登录后后端签发 token，推荐 `JWT + HttpOnly Cookie`，或者先用 Bearer Token 也可以
- 前端增加登录页、退出登录、登录状态检查

用户表至少要有这些字段：
- `id`
- `email`
- `password_hash`
- `nickname`
- `created_at`
- `updated_at`

**第二步：把 JSON 存储改成数据库**
这是核心改造。现在的 `server/src/storage.ts` 整套 `loadState/saveState` 要逐步退出，改成按表查询。

建议数据库：
- 开发和小规模上线：`PostgreSQL`
- 不建议继续用本地 JSON
- 也不建议一开始就自己拼 SQL 字符串，最好上 ORM

建议技术栈：
- ORM：`Prisma`
- 数据库：`PostgreSQL`
- 部署数据库：`Neon`、`Supabase Postgres`、`Railway Postgres` 都可以

你至少需要这些表：

1. `users`
- 用户账号

2. `tasks`
- 每个任务归属一个用户
- 关键字段：
  - `id`
  - `user_id`
  - `title`
  - `detail`
  - `progress`
  - `eta`
  - `owner_note`
  - `created_at`
  - `updated_at`
  - `completed_at`
  - `completion_reflection`
  - `status`

3. `task_milestones`
- 一个任务多个里程碑
- 关键字段：
  - `id`
  - `task_id`
  - `title`
  - `weight`
  - `sort_order`

4. `task_progress_logs`
- 任务推进日志
- 关键字段：
  - `id`
  - `task_id`
  - `summary`
  - `progress_before`
  - `progress_after`
  - `increase`
  - `created_at`

5. `inspirations`
- 灵感池中的灵感
- 关键字段：
  - `id`
  - `user_id`
  - `source_text`
  - `title`
  - `task_description`
  - `eta`
  - `state`
  - `created_at`
  - `updated_at`

6. `inspiration_milestones`
- 灵感拆解时的里程碑草稿
- 关键字段：
  - `id`
  - `inspiration_id`
  - `title`
  - `weight`
  - `sort_order`

7. `inspiration_conversations`
- 灵感补充时和 AI 的上下文记录
- 关键字段：
  - `id`
  - `inspiration_id`
  - `role`
  - `content`
  - `created_at`

如果你想做得更规范，还可以加：
- `user_settings`
- `ai_call_logs`
- `audit_logs`

**第三步：把所有接口改成“按当前用户读写”**
这是账号隔离真正生效的地方。

现在接口大概是这种思路：
- 读取整份全局状态
- 修改整份全局状态
- 再写回 JSON

以后应该变成：
- 先从 token 里拿到当前用户 `userId`
- 所有查询都必须带 `where user_id = currentUser.id`
- 所有新增数据都自动写入 `user_id`

比如这些接口都要改：
- 获取灵感列表
- 保存灵感草稿
- AI 拆解灵感
- 创建任务
- 获取执行中心任务
- 保存任务编辑
- 确认推进
- 完成任务并记录心得
- 获取成就中心数据

关键原则只有一条：
- 后端永远不能信任前端传来的“这个任务属于谁”
- 必须后端自己根据登录用户去查
- 任何 `taskId`、`inspirationId` 查询都必须验证归属关系

例如伪逻辑应该是：
```ts
const task = await db.task.findFirst({
  where: {
    id: taskId,
    userId: currentUser.id
  }
});
```

而不是：
```ts
const task = await db.task.findUnique({ where: { id: taskId } });
```

**第四步：把 AI 调用和业务数据绑定**
你现在已经有灵感拆解、今日进度分析，这部分上线后也要变成“按用户上下文工作”。

需要做的事情：
- AI 拆解灵感时，读取当前用户该条灵感的历史补充记录
- AI 生成今日进度时，只读取当前用户的任务列表
- 用户编辑任务后，要把编辑后的任务信息写回数据库，作为后续 AI 分析输入
- 可以额外记录每次 AI 调用日志，方便排查问题

建议新增一张表：
- `ai_generation_logs`
字段例如：
- `id`
- `user_id`
- `related_type`，比如 `task` / `inspiration`
- `related_id`
- `prompt_excerpt`
- `response_excerpt`
- `model`
- `created_at`

不是必须，但上线后非常有用。

**第五步：前端要接入真实登录态**
前端要补这些能力：
- 登录页
- 注册页
- 右上角“登录/设置”改成真实用户头像或邮箱
- 页面刷新后自动恢复登录态
- 未登录时跳转登录
- token 失效时自动退出

如果你用 Cookie 方案：
- 前端请求要带 `credentials: "include"`

如果你用 Bearer Token：
- 前端要统一在请求头加 `Authorization: Bearer xxx`

你现在的 `client/src/api.ts` 会是主要改造点。

**第六步：做公网部署**
要公开可访问，至少要把前后端和数据库分别放到可访问环境。

常见部署组合：
- 前端：Vercel / Netlify
- 后端：Railway / Render / Fly.io / 云服务器
- 数据库：Neon / Supabase / Railway Postgres

一个很常见、成本也不高的组合是：
- 前端放 `Vercel`
- 后端放 `Railway`
- 数据库用 `Neon Postgres`

你需要配置：
- 后端环境变量
- 数据库连接字符串
- 前端 API 地址
- CORS 白名单
- 生产环境域名

至少会有这些环境变量：
- `DATABASE_URL`
- `JWT_SECRET`
- `OPENAI_API_KEY`
- `OPENAI_MODEL`
- `OPENAI_BASE_URL`
- `CLIENT_ORIGIN`

**第七步：补安全和上线必需项**
这一步非常重要，不然即使能跑，也不适合公开。

至少要做：
- 密码哈希，不存明文
- CORS 限制来源
- 接口鉴权中间件
- 输入校验，推荐 `zod`
- 请求限流，防止接口被刷
- AI 接口限流，防止成本失控
- 日志脱敏，不打印密钥和密码
- `.env` 管理敏感信息
- HTTPS
- 数据库备份

建议中间件：
- `helmet`
- `cors`
- `express-rate-limit`
- `zod`

**第八步：做数据迁移**
现在你的数据都在 JSON 里，如果不想丢掉，可以做一次迁移脚本。

迁移方式：
1. 先建立数据库表
2. 新建一个默认用户
3. 把当前 `app-state.json` 中的 `tasks`、`bubbles`、`progressLogs`、`milestones` 导入数据库
4. 后续停止使用 JSON 文件

这一步我可以帮你直接写脚本，把现有本地数据迁过去。

**建议你按这个顺序落地**
最稳的顺序是：

1. 引入 PostgreSQL + Prisma
2. 设计数据库表
3. 把任务/灵感/日志迁移到数据库
4. 增加用户表和登录注册
5. 所有接口按 `userId` 隔离
6. 前端接入真实登录
7. 部署到公网
8. 补限流、审计、备份

**对你这个项目的最小可上线版本**
如果目标是尽快可用，我建议先做一个 MVP 版本，不要一上来做太重：

- 用户注册/登录
- PostgreSQL 存储
- 任务、里程碑、进度日志、灵感池全部按用户隔离
- AI 拆解和今日进度分析接数据库
- 前端登录态
- 后端部署到公网
- 基础限流和 CORS

先不要做：
- 第三方登录
- 多组织/团队协作
- 复杂权限系统
- WebSocket 实时同步
- 复杂消息中心

**如果你要我直接开始改，我建议下一步这样做**
我可以直接在你当前项目里开始第一阶段的正式改造，顺序如下：

1. 接入 `PostgreSQL + Prisma`
2. 设计并创建 `users/tasks/task_milestones/task_progress_logs/inspirations/...` 表
3. 保留现有前端页面不大改，先把后端数据源从 JSON 切到数据库
4. 再补登录注册和鉴权

如果你愿意，我下一条就可以直接开始做数据库版的第一步：
- 先在项目里接入 Prisma
- 写出数据库 schema
- 保留现有业务结构，方便平滑迁移